# {{ ansible_managed }}

# Base settings
local   all             postgres                                peer
local   all             all                                     peer
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5

# Replication settings
host    replication     {{ pgsql_user }}       {{ pgsql_net }}            md5
host    all             {{ pgsql_user }}       {{ pgsql_net }}            md5
